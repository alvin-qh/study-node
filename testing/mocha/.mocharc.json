{
  "diff": true,
  "env": "dotenv_config_path=.env",
  "exit": true,
  "extension": [
    "ts", "js"
  ],
  "recursive": true,
  "reporter": "spec",
  "require": [
    "dotenv/config",
    "ts-node/register",       // 支持 typescript
    "tsconfig-paths/register" // 支持 typescript 的路径别名
  ],
  "slow": 75,
  "timeout": 5000,
  "ui": "bdd"
}
